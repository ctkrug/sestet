// Placeholder shell for the SCOPE phase — proves the app boots and the
// API route wiring works. The real wall/composer UI lands in BUILD,
// following docs/DESIGN.md.
export default function HomePage() {
  return (
    <main>
      <h1>Six Words Live</h1>
      <p>Scaffold running. The live wall lands in the next build pass.</p>
    </main>
  );
}

# Local dev via `wrangler pages dev` reads bindings from wrangler.toml directly —
# no env vars are required for D1/KV locally. This file documents the shape for
# anything added later (e.g. an external service key) so secrets never get
# committed as literal values.
